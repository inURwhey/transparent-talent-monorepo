{
  "$schema": "https://turborepo.com/schema.json",
  "pipeline": {
    "build": {
      // ... other settings
    },
    "transparent-talent-frontend#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [
        "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY",
        "CLERK_SECRET_KEY"
      ]
    },
    // ... other settings
  }
}